"""
Private implementation detail of the `cube` package.

Nothing in `cube.internal` is covered by any compatibility guarantee and
its contents may be restructured or removed without notice. Code outside
`cube/` (including companion projects such as a solver, web app, CLI, or
MCP server) must import only from the top-level `cube` package.
"""
